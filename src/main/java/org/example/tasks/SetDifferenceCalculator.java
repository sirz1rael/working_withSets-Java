package org.example.tasks;

import java.util.HashSet;
import java.util.Set;

public class SetDifferenceCalculator {
    public Set<Integer> calculateDifference(Set<Integer> set1, Set<Integer> set2) throws Exception {
        if (set1.isEmpty() || set2.isEmpty()) throw new Exception("One of arguments is empty :(");

        Set<Integer> setsDifference = new HashSet<>();

        // returns elements of first set, which are missing in second set

        

        return setsDifference;
    }
}
